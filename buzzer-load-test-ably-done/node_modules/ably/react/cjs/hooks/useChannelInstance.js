"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.useChannelInstance = void 0;
const react_1 = __importDefault(require("react"));
const AblyContext_js_1 = require("../AblyContext.js");
function useChannelInstance(ablyId = 'default', channelName) {
    const { _channelNameToChannelContext, _nearestChannelName } = react_1.default.useContext(AblyContext_js_1.AblyContext)[ablyId];
    // When no channel name is provided, resolve to the channel named by the
    // closest enclosing `ChannelProvider`.
    const resolvedChannelName = channelName !== null && channelName !== void 0 ? channelName : _nearestChannelName;
    if (resolvedChannelName === undefined) {
        throw new Error('No channel name was provided and there is no parent ChannelProvider to infer one from. Make sure your channel based hooks (usePresence, useChannel, useChannelStateListener) are called inside a <ChannelProvider> component, or pass a channel name explicitly');
    }
    const channelContext = _channelNameToChannelContext[resolvedChannelName];
    if (!channelContext) {
        throw new Error(`Could not find a parent ChannelProvider in the component tree for channelName="${resolvedChannelName}". Make sure your channel based hooks (usePresence, useChannel, useChannelStateListener) are called inside a <ChannelProvider> component`);
    }
    return Object.assign(Object.assign({}, channelContext), { channelName: resolvedChannelName });
}
exports.useChannelInstance = useChannelInstance;
//# sourceMappingURL=useChannelInstance.js.map